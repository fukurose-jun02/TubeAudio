###### 対応内容

- YouTube検索機能（Issue #8）に続き、「サーバー起動でいつも苦労している」という当初の相談に対応
- Issue [#12](https://github.com/fukurose-jun02/TubeAudio/issues/12)を作成し、`docs/`3文書をこの件の内容で上書き（ユーザー承認済み）して要件定義〜実装計画を実施

###### 変更内容

- `server/com.fukurose.tubeaudio.plist`（新規）: launchd用LaunchAgentテンプレート。`RunAtLoad`でログイン時自動起動、`KeepAlive`でプロセス終了時に自動再起動
- `server/setup.sh`: セットアップ時に「Macのログイン時にサーバーを自動起動しますか？」と確認し、`y`ならpython3パス・プロジェクトパスを実環境値に置換して`~/Library/LaunchAgents/`へ登録するようにした
- `server/README.md`: 自動起動の使い方・アンインストール手順、Bonjourホスト名接続の案内を追記
- `.gitignore`: `server/run.log`を追加
- `ios/TubeAudio/APIClient.swift`: デフォルト接続先をIPアドレスからBonjourホスト名（`http://fukuroseatsushinomacbook-air.local:5001`）に変更。DHCPでIPが変わっても追従する
- `ios/TubeAudio/ContentView.swift`: 設定画面の文言をホスト名接続にも対応させて更新
- バージョンを1.06→1.07に更新

###### 検証内容

- `xcodebuild ... -sdk iphonesimulator build` で `BUILD SUCCEEDED`
- `bash -n server/setup.sh`・`plutil -lint server/com.fukurose.tubeaudio.plist` で構文確認
- ユーザー許可のもと、このMacに実際にLaunchAgentを登録（`launchctl load -w`）
  - `launchctl list`でプロセスが起動していることを確認
  - `curl http://localhost:5001/`・`curl http://fukuroseatsushinomacbook-air.local:5001/` いずれもHTTP 200
  - `kill -9`でプロセスを強制終了後、自動的に再起動することをPIDの変化で確認
  - iOS SimulatorでUI文言・バージョン表示（1.07）を確認

###### 成果物

- GitHub PR: [#13 feat: サーバーの自動起動とBonjourホスト名接続に対応](https://github.com/fukurose-jun02/TubeAudio/pull/13)（マージ済み、`Closes #12`）

###### マージ

- ユーザーがPR #13をマージ（2026-09-22）。`Closes #12`によりIssue #12も自動クローズ
- ローカルmainを最新化し、`feature/server-autostart`ブランチ（ローカル・リモート）を削除
- `docs/`3文書を完了状態に更新

###### 次回やること

- 実機iPhoneでの`.local`ホスト名接続の最終確認は任意（Mac自身からの`curl`とSimulatorでは確認済み）
- これでYouTube検索機能・サーバー起動自動化の2件とも完了。次の相談は特になし
