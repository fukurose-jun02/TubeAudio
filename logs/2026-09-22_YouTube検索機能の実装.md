###### 対応内容

- [2026-09-22_YouTube検索機能の計画.md](2026-09-22_YouTube検索機能の計画.md)で作成した開発文書に基づき、YouTube検索機能を実装
- ユーザーがGoogle Cloud ConsoleでYouTube Data APIキーを発行し、`server/.env`に設定
- サーバー・iOS双方を実装し、iOS Simulatorで実際に検索→変換→ライブラリ保存まで動作確認

###### 変更内容

- `server/app.py`: `/api/search`エンドポイントを追加。`python-dotenv`で`server/.env`から`YOUTUBE_API_KEY`を読み込み、YouTube Data API v3の`search.list`を呼び出す。タイトル・チャンネル名のHTMLエンティティ（`&#39;`等）をデコードして返す処理を追加
- `server/requirements.txt`: `requests`・`python-dotenv`を追加
- `server/README.md`: `/api/search`のドキュメントとAPIキー設定手順を追記
- `ios/TubeAudio/APIClient.swift`: `SearchResult`構造体・`search(query:)`メソッドを追加
- `ios/TubeAudio/SearchView.swift`（新規）: 検索タブ。結果をタップすると即座に変換を開始し、完了後ライブラリに保存する
- `ios/TubeAudio/ContentView.swift`: `TabView`に「検索」タブを追加
- バージョンを1.05→1.06に更新（アプリ開発ルールに準拠）
- `docs/requirements.md`・`docs/implementation-plan.md`を実装完了状態に更新（main直接コミット）

###### 検証内容

- `xcodebuild ... -sdk iphonesimulator build` で `BUILD SUCCEEDED`
- `curl "http://localhost:5001/api/search?q=lofi"` で検索結果とHTMLエンティティのデコードを確認
- iOS Simulatorで実際に「lofi」を検索→結果タップ→ダウンロード（0%→33%→62%→85%）→変換→「ライブラリに保存されました」→ライブラリタブで発信者アイコン・再生時間（2:50:40）・投稿日・再生回数（929.9万回再生）が表示されることを確認
- テストで作成したダウンロードファイル（サーバー側・iOSアプリ側とも）は確認後に削除済み

###### 成果物

- GitHub PR: [#10 feat: YouTube動画のキーワード検索機能を追加](https://github.com/fukurose-jun02/TubeAudio/pull/10)（マージ待ち、`Closes #8`）

###### 次回やること

- PR #10のレビュー・マージ
- 実機（Mac+iPhone同一WiFi）での最終確認
- 別件：「サーバー起動の苦労」の改善（launchd自動起動など）は未着手のまま
